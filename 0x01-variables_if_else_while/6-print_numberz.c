#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int c = 30;

	while (c <= 39)
	{putchar(c++);
	}
	putchar('\n');

	return (0);
}
